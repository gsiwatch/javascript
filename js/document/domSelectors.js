// single element or multiple element selectors
/**
 * single element selector
 */
// document.getElementById(); -- single element selector

// afterwards in case we want to get attributes of this element then we can do something like following:
// document.getElementById('<id>').className;  this will give the className
// document.getElementById('<id>').style.background = '#333'; this will change the background color of that id
// document.getElementById('<id>').textContent = '#333'; this will change the background color of that id
